services:
  db2:
    image: mysql/mysql-server:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: flappy_fish
      MYSQL_DATABASE: BoatsAndSailors
      MYSQL_ROOT_HOST: '%'
    volume:
      - basdat2:/var/lib/mysql
    ports:
      - "53326:3306"

volumes:
  basdat2:
